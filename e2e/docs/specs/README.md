# e2e/docs/specs

Feature specs for this package (prose, written before/alongside a new flow).
Not the same as [`../../specs/`](../../specs/), which holds the JSON flow-test
fixtures consumed by `run.ts`.
